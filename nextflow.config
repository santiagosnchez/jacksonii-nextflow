params {
    from_sra = null
    outputDir = "${projectDir}/data"
    reads_dir = "${params.outputDir}/reads"
    tmp_dir = "${projectDir}/.tmp"
}

process {
    executor = 'local'
    maxForks = 2
}

docker {
    enabled = true
    options = '-v ${params.tmp_dir}:/tmp'
}

